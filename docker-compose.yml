version: '3'

services:
  postgres:
    image: 'postgres'
    volumes:
      - 'postgres:/var/lib/postgresql/data'

  app:
    depends_on:
      - 'postgres'
    build: .
    command: bash start.sh
    ports:
      - '3000:3000'
      - '5000:5000'
      - '3035:3035'
    volumes:
      - '.:/app'
      - 'gems/:/gems'

volumes:
  postgres:
  gems: